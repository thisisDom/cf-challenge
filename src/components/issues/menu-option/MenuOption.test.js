import React from 'react';
import { shallow } from 'enzyme';
import MenuOption from './MenuOption';

  it('<MenuOption /> renders without crashing', () => {
      shallow(<MenuOption />)
  });

  it('Renders the Author menu option', () => {

  });

  it('Renders the Label menu option', () => {

  });

  it('Renders the Sort menu option', () => {

  });
